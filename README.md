# solution-powertoggle-vm

PowerToggle VM starts and deallocates Azure virtual machines from schedules defined in resource tags. Azure Functions process tag changes, run due start or stop actions, and extend future scheduling so VM runtime can be controlled automatically.
